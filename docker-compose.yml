services:
  sistur-flask:
    build: .
    container_name: sistur-flask-app
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8083}:5000"
    restart: unless-stopped
    networks:
      - sistur-network

networks:
  sistur-network:
    external: true
